{
  "builds": [
    { "src": "*.html", "use": "@vercel/static" },
    { "src": "*.js", "use": "@vercel/node" }
  ],
  "routes": [{ "src": "/.*", "dest": "/index.js" }],
  "env": {
    "MONGO_URI": "$MONGO_URI"
  }
}
